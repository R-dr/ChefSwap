<nav class="navbar navbar-expand-lg navbar navbar-light">
  <a class="navbar-brand" href="/">ChefSwap</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      
      <li class="nav-item pr-4">
        <%= link_to 'Your bookings', bookings_index_path, class: 'btn btn-outline-dark' %>
      </li>
       <li class="nav-item pr-4">
         <%= link_to 'View listings', listings_path, class: 'btn btn-outline-dark' %>
      </li>
      <% if user_signed_in? && current_user.is_chef? %>
      <li class="nav-item pr-4">
         <%= link_to 'Recipes', recipes_path, class: 'btn btn-outline-dark' %>
      </li>
      <% end %>
      <% if user_signed_in? %>
      <li class="nav-item pr-4">
        <%= link_to 'Edit User profile', edit_user_registration_path, class: 'btn btn-outline-dark' %>
      </li>
      <li class="nav-item pr-4">
      <%= link_to'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-outline-dark ' %> 
      </li>
      <% else %>
      <li class="nav-item pr-4">
      <%= link_to 'Sign-up', new_user_registration_path,class:"btn btn-outline-success" %>
      <% end %>
      
    </ul>
  </div>
</nav>